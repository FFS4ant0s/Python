'''

.upper() = Letra maiúscula.
continue = pula para o proximo laço.
break = termina o nosso laço.
while = usado quando não se sabe quantas repetições (enquanto).
for = usado quando o número de reptições é especifico.
# From math import factorial = calculo do fatorial (!).
.average() = Media aritimética.
.count(x) = quantas vezes 'x' aparece na string
.startswith() = checa se começa com determinada letra.
.lower() = Diminuir para minusculo.
.count() = quantas palavra exitem.
.sort() = Ordem Crescente.
.sort(reverse=True) = Ordem Decrescente.
.reverse() = Mostra em ordem invertida ou então usar fomula. [::-1]
.append() = Inserir um valor ao final.
insert() = inserir em um determinado lugar.
pop() = Deleta o último elemento da lista.
del() = Deletar.-+-+-
min() = Minimo.
max() = Maximo.
.isdigit() = verifica se tudo digitado é número.
insert(,) = Inserir o caractere em um determinado lugar.
! (Import statistics) statistics.mean() = Média da lista.
.isalpha() = Checar se tudo digitado é  letra.
sum() = somando todos os elementos de uma lista.
! (Import numpy as numpy) numpy.prod()  = a multiplicação de todos os termos da lista.
! (Import itertools) list(itertools.chain(*zip() = Intercalação entre as listas.
.replace(e,3) = todo valor 'e' vai mudar para 3.
sep= = SEPARADOR.
items() = é usado para retornar a lista com todas as chaves do dicionário.
/t = Paragrafo.
fillvalue = ao invés de none defina uma string.
(Import itertools import count)count = contador.
(Import itertools import groupby)groupby = agrupamento.
round() = número de casas decimais.
.center(x) = centralizar.  (x)= quantas cassas.

'''